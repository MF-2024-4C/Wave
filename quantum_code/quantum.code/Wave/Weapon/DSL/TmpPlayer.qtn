component TmpPlayer
{

}

event Fire {
   PlayerRef Player;
}

event ChangeActiveWeapon {
   PlayerRef Player;
   asset_ref<WeaponData> NewWeapon;
}

event InstanceWeapon {
   PlayerRef Player;
   asset_ref<WeaponData> Weapon;
}

event ChangePrimaryWeapon {
   PlayerRef Player;
}

event ChangeSecondaryWeapon {
   PlayerRef Player;
}

event ChangeTertiaryWeapon {
   PlayerRef Player;
}
