asset HealthConfig;

component HealthComponent
{   
    asset_ref<HealthConfig> Config;
    
    FP CurrentHealth;
}

signal OnAddHealth(entity_ref entity, FP amount);
signal OnDecHealth(entity_ref entity, FP amount);
signal OnDead(entity_ref entity);